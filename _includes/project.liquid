---
layout: main
---

<article>
  <h2>{{ title }}</h2>
  <p>
    <i class="dates">
      {{ date | date: "%Y" }} {% if dateTo %} - {{ dateTo | date: "%Y" }}
      {% endif %}
    </i>
  </p>
  {{ content }}
</article>
