---
layout: main
---

<article>
  <h2>{{ title }}</h2>
  <p>
    <i>{{ dateFrom | date: "%Y" }}</i
    >{% if dateTo %} <i> - {{ dateTo | date: "%Y" }}</i
    >{% endif %}
  </p>
  {{ content }}
</article>
