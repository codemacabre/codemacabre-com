---
layout: main
---

<article>
  <h2>{{ title }}</h2>
  <p>
    <i class="dates">
      Written {{ date | date: "%d %b, %Y" }} {% if update %} / Updated
      {{ update | date: "%d %b, %Y" }}
      {% endif %}
    </i>
  </p>
  {{ content }}
</article>
