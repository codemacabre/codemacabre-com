---
layout: main
---

<article>
  <h2>{{ title }}</h2>
  <p>
    <i>Written {{ date | date: "%d %b, %Y" }}</i
    >{% if update %} <i> / Updated {{ update | date: "%d %b, %Y" }}</i
    >{% endif %}
  </p>
  {{ content }}
</article>
