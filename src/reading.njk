---
layout: base.njk
title: Reading
description: A (selected) view of Myles Lewando's non-fiction bookshelf
---

<h2>Reading</h2>
<p>This is a selection of my non-fiction audiobooks, ebooks, paperbacks, and hardbacks.</p>
<p>I use <a href="https://libro.fm/referral?rf_code=lfm227162">Libro.fm</a> for audiobooks and try to avoid ebooks with DRM whenever possible. Titles here link to the Internet Archive's <a href="https://openlibrary.org">Open Library</a> to encourage readers to borrow books if available.</p>
<ul>
{% for item in books %}
  <li class="card">
    <img class="greyscale" src="{{ item.cover.src }}" alt="{{ item.cover.alt }}" />
    <p><a href="{{ item.url }}">{{ item.title }}</a> by 
    {% for entity in item.author %}
      {% if item.author.length > 1 %}
        {% if entity === item.author[item.author.length - 1] %}
          {{ entity }}
        {% else %}
          {{ entity }}, 
        {% endif %}
      {% else %}
        {{ entity }}
      {% endif %}
    {% endfor %}
    </p>
    <div class="status-container">
    {% if item.status !== 'to read' %}
      <div class="status">{{ item.status}}</div>
    {% endif %}
    </div>
  </li>
{% endfor %}
</ul>
