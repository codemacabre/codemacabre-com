---
layout: base.njk
title: Book notes
description: Book notes by Myles Lewando
pagination:
  data: collections.booknotes
  size: 10
  reverse: true
---

<h2>{{ title }}</h2>
<ul class="wider">
{% for post in pagination.items %}
  <li>
    <a class="title" href="{{ post.url }}">{{ post.data.title }}</a> <span class="dates">(<time datetime="{{ post.data.date }}">{{ post.data.date | date('full') }}</time>)</span>
    <p class="description">{{ post.data.description }}</p>
  </li>
{% endfor %}
</ul>

{% include 'pagination.njk' %}
